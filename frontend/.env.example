# BioTwin Frontend Environment Variables
# Copy this file to .env and fill in the values

# API Configuration
NEXT_PUBLIC_API_BASE_URL=http://localhost:5000/api

# External Integration Keys (if needed for direct frontend calls)
# Note: Avoid putting sensitive keys in frontend - use backend proxy instead
NEXT_PUBLIC_HMS_API_KEY=your-hms-api-key-here
